import { LavaJsOptions } from "../types";

export const defaultOptions = {
  autoRun: true,
  datetimeFormat: "",
  debounceTimeout: 250,
  locale: "en",
  mapsApiKey: "",
  responsive: true,
  timezone: "America/Los_Angeles"
} as LavaJsOptions;
